[tool.poetry]
name = "repl_python3_Discord-Bots"
version = "0.1.0"
description = ""
authors = ["SubhoBoy <<>>"]

[tool.poetry.dependencies]
python = "^3.10"
discord = "^1.7.3"

[tool.poetry.dev-dependencies]
"discord.py-stubs" = "^1.7.3"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
